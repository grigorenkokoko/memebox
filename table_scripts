CREATE TABLE user_registration
(
    id SERIAL PRIMARY KEY,
    e_mail CHARACTER VARYING(30) NOT NULL UNIQUE,
    password CHARACTER VARYING(30),
	name CHARACTER VARYING(30),
	surname CHARACTER VARYING(30),
    Age INTEGER
);

CREATE TABLE mem
(
    id SERIAL PRIMARY KEY,
    mem TEXT UNIQUE,
    mem_type CHARACTER VARYING(30),
    likes INTEGER,
	dislikes INTEGER
);

CREATE TABLE category
(
    id SERIAL PRIMARY KEY,
    category CHARACTER VARYING(30) UNIQUE
);


CREATE TABLE user_registration_category
(
    user_registration_id INTEGER REFERENCES user_registration(id),
    category_id INTEGER REFERENCES category(id),
    CONSTRAINT user_registration_category_pk PRIMARY KEY (user_registration_id, category_id)
);


CREATE TABLE mem_category
(
    mem_id INTEGER REFERENCES mem(id),
    category_id INTEGER REFERENCES category (id),
    CONSTRAINT mem_category_pk PRIMARY KEY (mem_id, category_id)
);
